# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.venv*/
.env
*.egg-info/
dist/
build/

# Next.js
apps/web/.next/
apps/web/out/

# Python
apps/api/.env
apps/api/__pycache__/

# Qdrant data (local dev)
qdrant_storage/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Local SQLite dev database
apps/api/dev.db
*.db-journal

# Test coverage
.coverage
htmlcov/
coverage/
.pytest_cache/

# Playwright
apps/web/playwright-report/
apps/web/test-results/

# Logs
*.log
npm-debug.log*
.vercel
.env*

# Eval harness generated reports (commit published results deliberately, not by default)
apps/api/eval/out/
apps/api/scripts/out/

# Cloud service-account credentials (never commit — e.g. Anya TTS service account JSON)
apps/api/secrets/

# Unrelated coursework artifacts that landed in the repo root (not part of WanderPlanner).
# Leading slash so these only ever match at the root, never a same-named path nested elsewhere.
/Agentic AI Capstone Deploy Companion v0.3_1.zip
/Agentic AI Capstone Deploy Companion v0.3_1/

# Agent/skill tooling (generic, reusable across projects — belongs in its own repo, not here)
.agents/
.claude/
AGENTS.md
Skills-Results/
agent/
docs/agents/
skills-lock.json
