# Heavy ML dependencies — install after core requirements
# Requires ~3GB disk space (PyTorch included)
langchain==0.2.5
langchain-groq==0.1.6
langchain-community==0.2.5
sentence-transformers==3.0.0

# Only needed to run eval/run_model_comparison.py (docs/eval-set.md §8,
# model-selection eval) against the respective provider — not used by the
# production app. Install only the ones matching the API keys you have.
groq==0.9.0
openai==3.2.0
anthropic==0.36.0

# Only needed if settings.youtube_stt_fallback_enabled is turned on (issue
# #38 — yt-dlp + local Whisper STT for YouTube videos with no caption
# track). Off by default, so not installed in the production Dockerfile;
# see scrapers/youtube_narration.py's STT-fallback section for the settings
# flags this gates behind. Also requires a local `ffmpeg` binary on PATH
# (not a pip package) for both yt-dlp's audio extraction and Whisper's own
# audio decoding.
yt-dlp==2024.12.13
openai-whisper==20240930
